{
  "name": "timeium",
  "version": "0.1.0",
  "private": true,
  "description": "Temporium — timesheet, approval, and payroll-reporting app. Runs standalone or alongside Attendium on the same Supabase project.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
